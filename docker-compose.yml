services:
  zmsp:
    container_name: zmsp
    hostname: zmsp
    restart: unless-stopped
    network_mode: bridge
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./data:/data
    ports:
      - 6638:6638
